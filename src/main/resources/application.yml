server:
  port: 9001
  shutdown: graceful
  tomcat:
    connection-timeout: 2s
    keep-alive-timeout: 15s
    threads:
      max: 50
      min-spare: 5

spring:
  application:
    name: welcome-svc
  lifecycle:
    timeout-per-shutdown-phase: 15s

portal:
  greeting: Welcome from src/main/resources/application.yml
